version: "2"
services:
  nginx:
    image: nginx:1.18.0
    pid: "host"
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - /etc/letsencrypt:/etc/letsencrypt
      - /var/log/nginx:/var/log/nginx
      - ./nginx.conf:/etc/nginx/nginx.conf
    environment:
      - SSLPATH=neocompiler.io-0001  
